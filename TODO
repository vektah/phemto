Tasks for alpha3 release.

This is an interface release. I.e. the code does not work in any exhaustive way,
it just illustrates the direction of the project.

In priority order...
--------------------
* Write a Singleton lifecycle.
* Add parameters to Reused lifecycle.
* Add parameters to Singleton lifecycle.
* Can fill dependencies with explicit values
